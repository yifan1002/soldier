/**
 * news模块接口列表
 */
import axios from './http'; // 导入http中创建的axios实例
import base from './base'; // 导入接口域名列表
// import qs from 'qs'; // 根据需求是否导入qs模块

const news = {
	// 新闻列表
	newsList(params) {
		return axios.get(`${base.url}/getWangYiNews`, {params});
	},
	// 其他接口…………
}

export default news;
