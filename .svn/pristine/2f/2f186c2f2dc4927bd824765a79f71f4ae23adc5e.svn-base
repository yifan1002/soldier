/**
 * 积分记录模块接口列表
 */
import axios from './http'; // 导入http中创建的axios实例

const mark = {
	// login登录 
	mark(params) {
		return axios.get(`/api/center/pointRecord`, params);
	}
	// 其他接口…………
}

export default mark;
